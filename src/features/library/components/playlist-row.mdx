import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as PlaylistRowStories from "./playlist-row.stories";

<Meta of={PlaylistRowStories} />

<div style={{ color: '#E8E8ED', fontFamily: 'system-ui, -apple-system, sans-serif', maxWidth: 680 }}>

# PlaylistRow

<p style={{ color: '#9898A3', fontSize: 16, marginBottom: 24 }}>
  A mix tape (playlist) entry in the library — icon thumbnail, name, track count, and disclosure chevron.
</p>

<Canvas of={PlaylistRowStories.PlaylistList} />

## Design rationale

Fermata calls playlists **mix tapes** — the name evokes personal, hand-curated collections. The playlist row represents one in a list context, such as the "Add to Playlist" sheet or the playlist section of the library.

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, margin: '24px 0' }}>
  <div style={{ padding: 16, backgroundColor: '#141416', borderRadius: 12 }}>
    <p style={{ fontWeight: 600, color: '#D4A0FF', marginBottom: 4 }}>Icon-based thumbnail</p>
    <p style={{ fontSize: 13, color: '#9898A3' }}>
      44×44 rounded square with a list icon on <code style={{ color: '#D4A0FF' }}>colors.elevated</code>. Unlike albums and artists, playlists use an icon instead of artwork — visually distinct from content items.
    </p>
  </div>
  <div style={{ padding: 16, backgroundColor: '#141416', borderRadius: 12 }}>
    <p style={{ fontWeight: 600, color: '#D4A0FF', marginBottom: 4 }}>Smart pluralization</p>
    <p style={{ fontSize: 13, color: '#9898A3' }}>
      "1 track" vs "12 tracks" — a small detail that makes the UI feel thoughtful. Local playlists append "· Local" to distinguish them from synced ones.
    </p>
  </div>
  <div style={{ padding: 16, backgroundColor: '#141416', borderRadius: 12 }}>
    <p style={{ fontWeight: 600, color: '#D4A0FF', marginBottom: 4 }}>Favourite indicator</p>
    <p style={{ fontSize: 13, color: '#9898A3' }}>
      A small accent heart when favourited, positioned before the chevron. Same pattern as track rows — accent for personal curation.
    </p>
  </div>
  <div style={{ padding: 16, backgroundColor: '#141416', borderRadius: 12 }}>
    <p style={{ fontWeight: 600, color: '#D4A0FF', marginBottom: 4 }}>Consistent list pattern</p>
    <p style={{ fontSize: 13, color: '#9898A3' }}>
      Same py-3 padding, flex layout, and chevron disclosure as ArtistRow. Consistent list rhythm across the app.
    </p>
  </div>
</div>

<Controls of={PlaylistRowStories.Default} />

## Variants

### Default
<Canvas of={PlaylistRowStories.Default} />

### Favourited
<Canvas of={PlaylistRowStories.Favourite} />

### Local playlist
<Canvas of={PlaylistRowStories.LocalPlaylist} />

Shows "· Local" suffix to indicate this playlist exists only on the device, not synced from a source.

### Single track
<Canvas of={PlaylistRowStories.SingleTrack} />

Correct singular: "1 track" not "1 tracks".

## Layout anatomy

```
┌──────┬──────────────────────────┬──┬──┐
│ [≡]  │ Playlist Name            │♥ │ ›│  py-3 px-1
│ 44×44│ 12 tracks                │  │  │
└──────┴──────────────────────────┴──┴──┘
  icon    flex-1                   opt  18
```

</div>
