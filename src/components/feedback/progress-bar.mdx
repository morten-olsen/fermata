import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as ProgressBarStories from "./progress-bar.stories";

<Meta of={ProgressBarStories} />

<div style={{ color: '#E8E8ED', fontFamily: 'system-ui, -apple-system, sans-serif', maxWidth: 680 }}>

# ProgressBar

<p style={{ color: '#9898A3', fontSize: 16, marginBottom: 24 }}>
  A thin linear progress indicator for podcast episodes, audiobook chapters, and playback position.
</p>

<Canvas of={ProgressBarStories.AllStates} />

## Design rationale

Progress is secondary information in Fermata — it should be visible but never compete with the content. The bar is thin (2px default), uses the accent color, and only appears when progress exists.

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, margin: '24px 0' }}>
  <div style={{ padding: 16, backgroundColor: '#141416', borderRadius: 12 }}>
    <p style={{ fontWeight: 600, color: '#D4A0FF', marginBottom: 4 }}>Minimal by default</p>
    <p style={{ fontSize: 13, color: '#9898A3' }}>2px height, rounded ends. Present but unobtrusive — it supplements the row content rather than dominating it.</p>
  </div>
  <div style={{ padding: 16, backgroundColor: '#141416', borderRadius: 12 }}>
    <p style={{ fontWeight: 600, color: '#D4A0FF', marginBottom: 4 }}>Accent color</p>
    <p style={{ fontSize: 13, color: '#9898A3' }}>Uses <code style={{ color: '#D4A0FF' }}>colors.accent</code> by default, matching the "now playing" visual language. Customizable for different contexts.</p>
  </div>
</div>

<Controls of={ProgressBarStories.Half} />

## Variants

### Progress states
<Canvas of={ProgressBarStories.Empty} />
<Canvas of={ProgressBarStories.Half} />
<Canvas of={ProgressBarStories.Full} />

### Custom styling
<Canvas of={ProgressBarStories.CustomColor} />

</div>
